CREATE  TABLE "main"."USER" ("USER_ID" INTEGER PRIMARY KEY  AUTOINCREMENT  NOT NULL  UNIQUE , "USER_USERNAME" TEXT NOT NULL , "USER_MAPS" INTEGER NOT NULL  DEFAULT 0, "USER_COMPLETED_MAPS" INTEGER NOT NULL  DEFAULT 0)

INSERT INTO USER
	(
	USER_USERNAME
	, USER_MAPS
	, USER_COMPLETED_MAPS
) VALUES (
	'Dani'
	, 3
	, 0
)

SELECT USER_USERNAME FROM USER WHERE USER_ID= 1

DELETE FROM USER WHERE USER_ID = 2